name: flutter_android_template
description: "A simple flutter template which integrated l10n, dynamic theme."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter
  intl: any

  dynamic_color: ^1.7.0
  provider: ^6.1.4
  shared_preferences: ^2.5.3

  cupertino_icons: ^1.0.8

  # 🔹 Sistem ayarlarını açmak için
  url_launcher: ^6.3.1

  # 🔹 Daha fazla ikon için (Samsung benzeri)
  font_awesome_flutter: ^10.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_launcher_icons: "^0.14.3"
  flutter_lints: ^5.0.0

flutter:
  generate: true
  uses-material-design: true

flutter_launcher_icons:
  android: "ic_launcher"
  ios: false
  image_path: "assets/icon/icon.png"
  min_sdk_android: 21
